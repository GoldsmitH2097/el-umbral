{
  "name": "el-umbral",
  "version": "1.0.0",
  "description": "El Umbral — A cinematic dark fantasy interactive experience.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "node scripts/generate-og-pages.js && node scripts/minify-tizno.cjs && node scripts/generate-tizno-pages.js && node scripts/generate-baby-tizno.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.75.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "stripe": "^22.6.1"
  }
}

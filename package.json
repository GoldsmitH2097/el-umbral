{
  "name": "el-umbral",
  "version": "1.0.0",
  "description": "El Umbral — A cinematic dark fantasy interactive experience.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "node scripts/generate-og-pages.js && node scripts/minify-tizno.cjs && node scripts/generate-tizno-pages.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "sass": "^1.75.0"
  }
}
